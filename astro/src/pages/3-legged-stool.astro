---
import Layout from "../layouts/Layout.astro";
import HeaderImage from "../components/HeaderImage.astro";
import UnderlinedHeader from "../components/UnderlinedHeader.astro";
import contactBg from "../../public/header-images/Contact-us.webp";
import stoolImg from "../../public/stool/stool.png";
const steps = [
    {
        step:1,
        title:"MAINTAIN YOUR APPOINTMENT SCHEDULE",
        description:"Your individualised care schedule is designed for the best results possible. Maintaining your care helps us to see the results we want to achieve."
    },  
    {
        step:2,
        title:"BREAK BAD HABITS",
        description:"Break the bad habits that cause and promote your postural break-down."
    },
    {
        step:3,
        title:"MAKE TIME TO MOVE",
        description:"Add movement so that you can hold your adjustments more effectively by giving strength to the muscles that support your posture."
    }
]
---

<Layout title="3-Legged Stool | Sydney Chiropractor CBD">
    <HeaderImage backgroundImage={contactBg.src} headerText="SYDNEY CBD CHIROPRACTORS" buttonText="Book an Appointment" />
    <!-- Two Column Grid Layout -->
    <div class="container mx-auto px-4 sm:px-6 lg:px-8 py-16">
        <UnderlinedHeader left={true}>REMEMBER THE “3”<br/>
            <span class="text-custom-purple text-5xl">THE 3 LEGGED STOOL</span></UnderlinedHeader>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8 mt-8">
            <div class="md:w-full md:col-span-1 md:pr-4 ">
                <h2 class="text-2xl font-bold">FACTS</h2>
                <p>A 3 legged stool cannot stand on less than 3 legs.
                    Each leg is equally important for success.</p>
                <br/>
                <p>Our plan for you is based on your specific goals and our recommendations. This is how you can help us get you the best results of your care.</p>
            </div>
            <div class="md:w-full md:col-span-1 md:pl-4 flex justify-center items-center">
                <img src={stoolImg.src} alt="3-Legged Stool" class="w-full h-auto rounded-lg" />
            </div>
        </div>
    </div>
    
    <!-- Steps Section -->
    <div class="container mx-auto px-4 sm:px-6 lg:px-8 py-16">
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8 text-center md:text-left">
            {steps.map((step) => (
                <div class="flex flex-col items-center md:items-start">
                    <div class="w-12 h-12 bg-custom-purple text-white rounded-full flex items-center justify-center text-xl font-bold mb-4">
                        {step.step}
                    </div>
                    <h3 class="text-lg font-bold text-custom-purple mb-3 uppercase">
                        {step.title}
                    </h3>
                    <p class="text-gray-600 leading-relaxed">
                        {step.description}
                    </p>
                </div>
            ))}
        </div>
    </div>
</Layout> 